# Notas:

Esta es la estructura inicial de un proyecto de Node.

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de multiplicar
                                                [number] [required] [default: 5]
  -l, --listar   Muestra el listado en consola        [boolean] [default: false]
  -h, --hasta    Maximo de la tabla a mostrar             [number] [default: 10]

```